apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-{{ .Values.environment }}-{{ .Values.sector }}-{{ .Values.region }}
  namespace: argocd
spec:
  project: default
  source:
    repoURL: {{ .Values.git_repo }}
    targetRevision: {{ .Values.git_revision }}
    path: experiments/kargo-progressive-rollout/helm/charts/myapp
    helm:
      parameters:
        - name: image.tag
          value: {{ .Values.apps.myapp.image.tag }}
  destination:
    server: https://kubernetes.default.svc
    namespace: myapp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
